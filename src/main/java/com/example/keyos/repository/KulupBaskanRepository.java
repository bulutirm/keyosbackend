package com.example.keyos.repository;

import com.example.keyos.model.KulupBaskan;
import org.springframework.data.jpa.repository.JpaRepository;

public interface KulupBaskanRepository extends JpaRepository<KulupBaskan, Long> {
}
